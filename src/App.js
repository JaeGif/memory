function App() {
  return (
    <div>
      {/* Header
          Scoreboard
          Lose/Win Conditionally rendered 
          CardContainer -- cards are stored in an array of components and the array is randomly filled with components after each click
                           thus changing where components are rendered. 
                           
            Cards   <Cards contain .png and name>
            Cards
            Cards
            ...
          
          Footer*/}
    </div>
  );
}

export default App;
